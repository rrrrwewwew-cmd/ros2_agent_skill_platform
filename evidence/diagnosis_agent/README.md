# Live diagnosis Agent evidence

`live_mimo_mcp_bge_v1.json` records the repository-safe subset of a real
Xiaomi MiMo plan followed by five official MCP stdio calls, isolated BGE-M3
retrieval and deterministic report materialization. API credentials and raw
full traces are excluded; local files are bound by SHA-256.
