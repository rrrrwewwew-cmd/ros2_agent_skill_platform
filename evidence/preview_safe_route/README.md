# Governed safe route preview evidence

`governed_release_v1.json` freezes the release, live rbot simulation, no-motion
check, and Registry-gated Runtime validation of `preview_safe_route@0.1.0`.

The evidence stores hashes and normalized metrics, not the private release key,
Registry database, mutable project-one map, or full runtime trace.
